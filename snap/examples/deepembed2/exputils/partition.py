import numpy as np
import os, sys, logging

def collect_partition_stats():
	print("collecting partition stats")

def get_partition_summary(graph_abbr, num_com):
	print("Gathering partition summary for {}, with {} partitions".format(graph_abbr, num_com))